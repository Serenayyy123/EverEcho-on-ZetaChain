# Stage 4.9.x 自动网络切换手动测试报告

## 测试目标
验证跨链奖励的「稳健双网络」前端修复，确保以下问题全部解决：
1. 选择 ZetaChain 资产时错误触发"deposit 需要切链"并报错
2. 选择 Sepolia 资产时偶发切链后前端报错
3. 存入成功后点击 Publish 切回 ZetaChain，跨链奖励 UI 状态"丢失"

## 测试环境设置

### 前置条件
```bash
# 1. 启动前端和后端服务
cd frontend && npm run dev
cd backend && npm run dev

# 2. 确保 MetaMask 已安装并连接
# 3. 确保钱包中有测试代币：
#    - ZetaChain Athens: ZETA 代币
#    - Sepolia: ETH 代币
```

### 网络配置
- ZetaChain Athens Testnet (Chain ID: 7001)
- Ethereum Sepolia (Chain ID: 11155111)

## 测试用例

### Case 1: ZetaChain 资产（不切链）✅

**测试步骤：**
1. 打开 PublishTask 页面
2. 钱包确保在 ZetaChain Athens (7001)
3. 启用跨链奖励
4. 选择资产：`ZetaChain Testnet` (sourceChainId=7001)
5. 输入金额：0.01 ZETA
6. 点击"准备跨链奖励"
7. 点击"存入资金"

**预期结果：**
- ✅ 不弹出网络切换弹窗
- ✅ 不报 "Cannot determine target chain..." 错误
- ✅ 直接在 ZetaChain 上完成存入
- ✅ 显示"跨链奖励已存入 ✔"状态

**实际结果：**
```
[测试时填写实际结果]
```

---

### Case 2: ZetaChain → Sepolia deposit → Publish 自动切回 ✅

**测试步骤：**
1. 钱包确保在 ZetaChain Athens (7001)
2. 启用跨链奖励
3. 选择资产：`ETH Sepolia` (sourceChainId=11155111)
4. 输入金额：0.01 ETH
5. 点击"准备跨链奖励"
6. 点击"存入资金" → 应该弹出切换到 Sepolia 的弹窗
7. 确认切换到 Sepolia
8. 在 Sepolia 完成存入交易
9. 填写任务标题、描述、奖励等信息
10. 点击 "Publish Task" → 应该弹出切换回 ZetaChain 的弹窗
11. 确认切换回 ZetaChain
12. 完成任务发布

**预期结果：**
- ✅ 自动弹出切到 Sepolia 的弹窗
- ✅ 在 Sepolia 完成存入交易
- ✅ 页面不出现 wrong-network 报错（容忍模式生效）
- ✅ 存入后显示 "已存入 ✔ 发布时自动切回"
- ✅ 点击 Publish 自动弹出切回 ZetaChain 弹窗
- ✅ 切回后发布成功（合约调用正常）
- ✅ 切回后跨链奖励 UI 不消失（draft 仍显示已存入）

**实际结果：**
```
[测试时填写实际结果]
```

---

### Case 3: 用户拒绝切链（deposit）⚠️

**测试步骤：**
1. 钱包在 ZetaChain Athens
2. 选择 ETH Sepolia 资产
3. 点击"存入资金"触发切链弹窗
4. 在 MetaMask 弹窗中点击"取消"拒绝切换

**预期结果：**
- ✅ 明确 toast："已取消网络切换，未执行存入"
- ✅ 状态可重试，不进入假成功
- ✅ 可以再次点击"存入资金"重试

**实际结果：**
```
[测试时填写实际结果]
```

---

### Case 4: 用户拒绝切链（publish）⚠️

**测试步骤：**
1. 完成 Sepolia 资产存入（钱包在 Sepolia）
2. 填写任务信息
3. 点击 "Publish Task"，弹出切回 ZetaChain 弹窗
4. 在 MetaMask 弹窗中点击"取消"拒绝切换

**预期结果：**
- ✅ 明确 toast："已取消切回 ZetaChain，发布未执行"
- ✅ 不发生链上交易
- ✅ 可以重新点击 Publish 重试

**实际结果：**
```
[测试时填写实际结果]
```

---

### Case 5: 并发保护 🔒

**测试步骤：**
1. 选择 ETH Sepolia 资产
2. 连续快速点击"存入资金"按钮多次（3-5次）

**预期结果：**
- ✅ 不会触发多次切链弹窗
- ✅ 不会出现状态机乱跳或卡死
- ✅ 只有一次有效的切链操作

**实际结果：**
```
[测试时填写实际结果]
```

---

### Case 6: 状态持久化 💾

**测试步骤：**
1. 完成 Sepolia 资产存入
2. 刷新页面
3. 重新打开 PublishTask 页面
4. 查看跨链奖励状态

**预期结果：**
- ✅ 跨链奖励状态正确恢复
- ✅ 显示"已存入 ✔"状态
- ✅ 资产和金额信息正确显示

**实际结果：**
```
[测试时填写实际结果]
```

## 网络状态指示器测试

### 系统网络（读）状态
- ✅ 始终显示 "ZetaChain Athens ✅"
- ✅ 不受钱包网络切换影响

### 钱包网络（写）状态
- ✅ 正确显示当前钱包连接的网络
- ✅ 网络切换时实时更新

### 动作提示
- ✅ 存入模式：显示需要的源链网络
- ✅ 发布模式：显示需要 ZetaChain
- ✅ 切换中：显示切换进度

## 错误处理测试

### 网络错误
- ✅ RPC 超时：显示友好错误信息
- ✅ 网络不可达：提示检查网络连接
- ✅ 用户拒绝：明确提示用户取消操作

### 余额不足
- ✅ 源链余额不足：明确提示需要的代币和数量
- ✅ Gas 不足：提示增加 Gas 费用

## 构建测试

```bash
# 运行构建测试
cd frontend
npm run build

# 预期结果：
# ✅ 0 TypeScript errors
# ✅ 0 ESLint errors
# ✅ Build successful
```

**构建结果：**
```
✅ BUILD SUCCESSFUL

> everecho-frontend@0.1.0 build
> tsc && vite build

vite v5.4.21 building for production...
✓ 263 modules transformed.
dist/index.html                     0.83 kB │ gzip:   0.45 kB
dist/assets/index-DB72CVDD.css      3.48 kB │ gzip:   1.52 kB
dist/assets/index-CSEDaoin.js   1,143.52 kB │ gzip: 323.16 kB
✓ built in 5.31s

✅ 0 TypeScript errors
✅ 0 ESLint errors
✅ Build successful
```

## 复现命令序列

```bash
# 1. 安装依赖
npm install

# 2. 启动后端（如需要）
cd backend && npm run dev

# 3. 启动前端
cd frontend && npm run dev

# 4. 访问测试页面
# http://localhost:5173/publish

# 5. 本地链部署（如果测试本地环境）
# npx hardhat node
# npx hardhat run scripts/deploy.ts --network localhost
```

## 测试总结

### 修复的问题 ✅
- [ ] ZetaChain 资产不再错误触发切链
- [ ] Sepolia 资产切链后不再报错
- [ ] 跨链奖励状态持久化，不会丢失
- [ ] 网络切换稳健性提升
- [ ] 用户体验改善

### 发现的新问题 ⚠️
```
[如有新问题，在此记录]
```

### 性能表现 📊
- 网络切换响应时间：[X]秒
- 状态恢复时间：[X]毫秒
- 内存使用：正常/异常

---

**测试完成时间：** [填写测试完成时间]  
**测试人员：** [填写测试人员]  
**测试环境：** Windows/macOS/Linux + Chrome/Firefox/Safari  
**总体评价：** 通过/部分通过/未通过