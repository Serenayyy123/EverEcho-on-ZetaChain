# Backend Environment Configuration
# Copy to .env and fill in your values

# Server Configuration
PORT=3001
NODE_ENV=development

# Database (if needed)
# DATABASE_URL=postgresql://user:password@localhost:5432/everecho

# AI Configuration
AI_PROVIDER=mock
# AI_PROVIDER=qwen
# AI_PROVIDER=openai

# AI API Keys (only needed for real providers)
# QWEN_API_KEY=your_qwen_api_key_here
# OPENAI_API_KEY=your_openai_api_key_here

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# 🔒 CODE FREEZE: AI services are completely off-chain
# ❌ No contract addresses, private keys, or RPC URLs in AI config
# ✅ AI only provides suggestions, never executes transactions